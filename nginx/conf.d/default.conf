server {
  listen 80;

  location / {
    root   /usr/share/nginx/html;
    index  index.html index.htm;
  }

  location /test2/ {
    root   /usr/share/nginx/html;
    index  index.html index.htm;
  }
  include conf.d/oracle.loc;
}